// Site configuration. Edit these values, then deploy.
window.ZINELY_CONFIG = {
  // Where to POST trial-signup form submissions. Drop in a Formspree endpoint
  // (https://formspree.io) or any service that accepts JSON via POST and
  // returns a 2xx on success. Leave empty to fall back to a mailto: link.
  formEndpoint: '',

  // Address used for the mailto: fallback and footer contact.
  contactEmail: 'hello@zinely.agency',

  // Telegram handle shown in the success state and footer.
  telegram: '@timzines',
};
